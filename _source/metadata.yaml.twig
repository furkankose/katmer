#file: noinspection YAMLDuplicatedKeys
targets:
    hosts:
        local:
            connection: local
        host_1:
            connection: ssh
            hostname: 192.168.1.102
            port: 22
            username: plusauth
            password: 1234

defaults:
    targets: east

tasks:
    - script: "echo {{ item }}"
      targets: local
      loop:
          - 1
          - 2
          - 3
      until:
        condition:  item > 2
        retries: 2
